<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="co.yedam.mapper.BoardMapper">

  <select id="boardList" resultType="co.yedam.vo.BoardVO">
    select * 
    from board_tbl 
    order by board_no
  </select>
  
  <select id="selectBoard" parameterType="int" resultType="co.yedam.vo.BoardVO">
    select *
  	from board_tbl
  	where board_no = #{boardNo}
  </select>
  
  <insert id="insertBoard" parameterType="co.yedam.vo.BoardVO">
    insert into board_tbl(board_no, title, content, writer)
    values (board_seq.nextval, #{title}, #{content}, #{writer})
  </insert>
  
  <select id="updateBoard" parameterType="co.yedam.vo.BoardVO">
    update board_tbl
  	set content = #{content}
  	where board_no = #{boardNo}
  </select>
  
   <select id="deleteBoard" parameterType="int" resultType="co.yedam.vo.BoardVO">
    delete board_tbl
  	where board_no = #{boardNo}
  </select>
  
</mapper>